# secrets
.env

# your actual resume and the profile derived from it
resume.*
my_resume.pdf
profile.json
!profile.example.json
# generated output
out/
*.html
!jobhunt/templates/*.html
!frontend/index.html

# personal resume files (keep derived profile and source documents local)
*_Resume.pdf

# Your dedupe index and application tracker. Personal, and it must never ship
# with the repo: a cloned seen.json marks every job as already-seen, so a new
# user's first run finds nothing. GitHub Actions persists it via actions/cache
# instead of committing it.
seen.json

# python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.pytest-tmp/
.mypy_cache/
.uv-cache/
.venv.broken*/

# local dashboard database and runtime diagnostics
jobhunt.sqlite3*
*.log
web-page-check.png

# frontend dependencies and generated production assets
frontend/node_modules/
frontend/dist/
frontend/.vite/

# editors / os
.vscode/
.idea/
.DS_Store
Thumbs.db
